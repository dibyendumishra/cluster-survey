# Core
supabase>=2.0.0
geopandas>=0.14.0
rasterio>=1.3.0
shapely>=2.0.0
numpy>=1.24.0
pandas>=2.0.0

# Clustering
scikit-learn>=1.3.0    # DBSCAN
hdbscan>=0.8.33        # HDBSCAN

# SAM (optional — only needed if you run sam_cluster.py)
# pip install git+https://github.com/facebookresearch/segment-anything.git
# pip install torch torchvision
